{
  "name": "med4u_connect",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "start": "concurrently -n BACKEND,FRONTEND -c cyan,magenta \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}


